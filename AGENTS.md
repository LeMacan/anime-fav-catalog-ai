# AGENTS.md — anime-fav-catalog-ai

**Status**: Empty project — SDD-managed, no code yet.

## Workflow

Follow the parent project workflow: see [`/home/quanxi/code/projects/AGENTS.md`](/home/quanxi/code/projects/AGENTS.md).

## This Project

- **Type**: Planned anime favorites catalog with AI features
- **Init**: Run `/sdd-init` before starting any SDD work
- **Skills**: Inherited from global skill registry (see `.atl/skill-registry.md`)

## Quick Start

1. Run `/sdd-init` to bootstrap SDD context
2. Use `/sdd-new <feature>` to start a new change

## Notes

- No existing code — start fresh with SDD workflow
- All SDD artifacts stored in Engram by default